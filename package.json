{
  "name": "phew-framework",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -f assets/build/css/main.*.min.css assets/build/js/app.*.js assets/build/js/app.*.js.map",
    "dev": "npm run clean && vite build --mode development",
    "build": "npm run clean && vite build --mode production",
    "watch": "npm run clean && vite build --watch --mode development"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "cssnano": "^6.1.2",
    "postcss": "^8.5.6",
    "sass": "^1.90.0",
    "vite": "^5.4.19",
    "vite-plugin-imagemin": "^0.6.1"
  },
  "description": "In command line run\r - npm install (On first use)\r - Run gulp (This watches JS / CSS changes and minfies files one compilied )",
  "main": "gulpfile.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.8"
  }
}
